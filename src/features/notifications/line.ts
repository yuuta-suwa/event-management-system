export async function pushLineMessage(lineUserId:string,message:string){const token=process.env.LINE_MESSAGING_ACCESS_TOKEN;if(!token)throw new Error("LINE_MESSAGING_ACCESS_TOKEN_NOT_CONFIGURED");const response=await fetch("https://api.line.me/v2/bot/message/push",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({to:lineUserId,messages:[{type:"text",text:message}]})});if(!response.ok)throw new Error(`LINE_API_${response.status}:${(await response.text()).slice(0,200)}`)}
