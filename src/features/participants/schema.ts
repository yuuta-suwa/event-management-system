import {z} from "zod";
export const registrationSchema=z.object({eventId:z.string().min(1),referralCode:z.string().optional(),quantity:z.coerce.number().int().min(1).max(10).default(1),name:z.string().trim().min(1).max(100),nameKana:z.string().trim().min(1).max(120),phone:z.string().trim().regex(/^[0-9+()-]{8,20}$/,"電話番号を確認してください"),email:z.string().trim().email(),gender:z.enum(["","MALE","FEMALE","OTHER"]).optional(),age:z.union([z.literal(""),z.coerce.number().int().min(0).max(120)]).optional(),occupation:z.string().trim().max(120).optional(),referrerName:z.string().trim().max(100).optional(),snsContact:z.string().trim().max(200).optional(),notes:z.string().trim().max(1000).optional(),privacy:z.literal("on")});
export type RegistrationInput=z.infer<typeof registrationSchema>;
export const updateMyDetailsSchema=z.object({token:z.string().min(1),name:z.string().trim().min(1).max(100),nameKana:z.string().trim().min(1).max(120),phone:z.string().trim().regex(/^[0-9+()-]{8,20}$/,"電話番号を確認してください"),email:z.string().trim().email(),gender:z.enum(["","MALE","FEMALE","OTHER"]).optional(),age:z.union([z.literal(""),z.coerce.number().int().min(0).max(120)]).optional(),occupation:z.string().trim().max(120).optional(),referrerName:z.string().trim().max(100).optional(),snsContact:z.string().trim().max(200).optional(),notes:z.string().trim().max(1000).optional()});
export type UpdateMyDetailsInput=z.infer<typeof updateMyDetailsSchema>;
