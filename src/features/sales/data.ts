import {db} from "@/server/db";
import {isLocalDemo} from "@/server/authz";
import {summarizeSales} from "./domain";
export async function salesData(year:number){if(isLocalDemo()){const events=[{id:"demo-autumn",name:"秋のビジネス交流会 2026",date:new Date("2026-10-18"),revenue:360000,paidCount:36,average:10000},{id:"demo-summer",name:"夏の交流会 2026",date:new Date("2026-07-12"),revenue:288000,paidCount:36,average:8000}];return assemble(year,events)}const rows=await db.event.findMany({where:{eventDate:{gte:new Date(`${year}-01-01T00:00:00+09:00`),lt:new Date(`${year+1}-01-01T00:00:00+09:00`)}},include:{tickets:{include:{payments:{orderBy:{createdAt:"desc"},take:1}}}},orderBy:{eventDate:"asc"}});const events=rows.map(e=>{const s=summarizeSales(e.tickets.map(t=>({paymentStatus:t.paymentStatus,amount:t.payments[0]?.amount??e.price})));return {id:e.id,name:e.name,date:e.eventDate,...s}});return assemble(year,events)}
function assemble(year:number,events:Array<{id:string;name:string;date:Date;revenue:number;paidCount:number;average:number}>){const months=Array.from({length:12},(_,i)=>({month:i+1,revenue:0}));for(const event of events)months[event.date.getMonth()].revenue+=event.revenue;const revenue=events.reduce((n,e)=>n+e.revenue,0),paidCount=events.reduce((n,e)=>n+e.paidCount,0);return {year,events:[...events].sort((a,b)=>b.revenue-a.revenue),months,revenue,paidCount,average:paidCount?Math.round(revenue/paidCount):0}}
