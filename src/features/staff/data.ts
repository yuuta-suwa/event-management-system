import { db } from "@/server/db";
import { isLocalDemo } from "@/server/authz";
export type AllocationRow={id:string;staffId:string;staffName:string;allocated:number;assigned:number;paid:number;checkedIn:number;referralCode:string};
export const demoAllocations:AllocationRow[]=[{id:"demo-a1",staffId:"demo-s1",staffName:"小澤",allocated:10,assigned:7,paid:6,checkedIn:5,referralCode:"ozawa-2026-autumn"},{id:"demo-a2",staffId:"demo-s2",staffName:"中村",allocated:8,assigned:6,paid:5,checkedIn:4,referralCode:"nakamura-2026-autumn"},{id:"demo-a3",staffId:"demo-s3",staffName:"伴",allocated:5,assigned:4,paid:4,checkedIn:4,referralCode:"ban-2026-autumn"}];
export async function listStaff(){if(isLocalDemo())return demoAllocations.map(a=>({id:a.staffId,displayName:a.staffName,email:`staff-${a.staffId}@example.com`,active:true}));const rows=await db.staffProfile.findMany({include:{user:{select:{email:true}}},orderBy:{displayName:"asc"}});return rows.map(r=>({id:r.id,displayName:r.displayName,email:r.user.email,active:r.active}))}
export async function listAllocations(eventId:string):Promise<AllocationRow[]>{if(isLocalDemo())return eventId==="demo-autumn-2026"?demoAllocations:[];const rows=await db.ticketAllocation.findMany({where:{eventId},include:{staff:true,tickets:{include:{checkin:true}}},orderBy:{staff:{displayName:"asc"}}});return rows.map(r=>({id:r.id,staffId:r.staffId,staffName:r.staff.displayName,allocated:r.allocatedQuantity,assigned:r.assignedQuantity,paid:r.tickets.filter(t=>t.paymentStatus==="PAID").length,checkedIn:r.tickets.filter(t=>t.checkin).length,referralCode:r.referralCode}))}
