import {Banknote,CalendarRange,ChartNoAxesColumnIncreasing,LockKeyhole,ReceiptJapaneseYen} from "lucide-react";
import {AppHeader} from "@/components/app-header";
import {salesData} from "@/features/sales/data";
import {requireUser} from "@/server/authz";
export default async function SalesPage({searchParams}:{searchParams:Promise<{year?:string}>}){await requireUser(["SUPER_ADMIN"]);const q=await searchParams;const year=/^20\d{2}$/.test(q.year??"")?Number(q.year):new Date().getFullYear();const data=await salesData(year);const max=Math.max(1,...data.months.map(m=>m.revenue));return <><AppHeader active="sales"/><main className="content-shell"><div className="page-heading"><div><p className="eyebrow">SUPER ADMIN ONLY</p><h1>売上管理</h1><p><LockKeyhole/>売上金額はスーパー管理者だけが閲覧できます。</p></div><form className="year-picker"><CalendarRange/><input name="year" type="number" min="2020" max="2100" defaultValue={year}/><button>表示</button></form></div><section className="sales-summary"><article><Banknote/><span>年間売上<strong>¥{data.revenue.toLocaleString("ja-JP")}</strong></span></article><article><ReceiptJapaneseYen/><span>入金件数<strong>{data.paidCount}件</strong></span></article><article><ChartNoAxesColumnIncreasing/><span>平均単価<strong>¥{data.average.toLocaleString("ja-JP")}</strong></span></article></section><section className="sales-grid"><article className="sales-chart"><h2>{year}年 月別売上</h2><div>{data.months.map(m=><span key={m.month}><i style={{height:`${m.revenue/max*100}%`}}/><b>¥{Math.round(m.revenue/10000)}万</b><small>{m.month}月</small></span>)}</div></article><article className="event-sales"><h2>イベント売上</h2>{data.events.map((e,i)=><div key={e.id}><b>{i+1}</b><span><strong>{e.name}</strong><small>{e.date.toLocaleDateString("ja-JP")}・入金{e.paidCount}件</small></span><em>¥{e.revenue.toLocaleString("ja-JP")}</em></div>)}</article></section></main></>}
