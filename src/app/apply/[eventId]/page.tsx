import {notFound} from "next/navigation";
import {CalendarDays,ExternalLink,MapPin,ShieldCheck,UserRound} from "lucide-react";
import {getPublicApplication} from "@/features/participants/public-data";
import {RegistrationForm} from "@/features/participants/registration-form";
export default async function ApplyPage({params,searchParams}:{params:Promise<{eventId:string}>;searchParams:Promise<{ref?:string}>}){const[{eventId},{ref}]=await Promise.all([params,searchParams]);const data=await getPublicApplication(eventId,ref);if(!data)notFound();const{event}=data;return <main className="public-shell"><header className="public-brand">EVENT MANAGEMENT SYSTEM</header><section className="application-layout"><aside className="application-event"><span className="status-badge status-published">受付中</span><p className="eyebrow">EVENT APPLICATION</p><h1>{event.name}</h1><div className="public-event-info"><p><CalendarDays/>{event.eventDate.toLocaleDateString("ja-JP",{year:"numeric",month:"long",day:"numeric",weekday:"short"})}<small>{event.startTime.toLocaleTimeString("ja-JP",{hour:"2-digit",minute:"2-digit"})}–{event.endTime.toLocaleTimeString("ja-JP",{hour:"2-digit",minute:"2-digit"})}</small></p><p><MapPin/>{event.venueName}<small>{event.address}</small></p><p><UserRound/>定員 {event.capacity}名</p></div><div className="price-panel"><span>参加費</span><strong>¥{event.price.toLocaleString("ja-JP")}</strong></div>{data.introducerName&&<div className="introducer">紹介スタッフ：<strong>{data.introducerName}</strong></div>}{event.promoUrl&&<a className="demo-ticket-link" href={event.promoUrl} target="_blank" rel="noreferrer"><ExternalLink size={16}/>イベント詳細ページを見る</a>}</aside><section className="application-form-card"><div><p className="eyebrow">YOUR INFORMATION</p><h2>参加者情報</h2><p>必要事項をご入力ください。</p></div><RegistrationForm eventId={eventId} referralCode={data.referralCode}/><p className="secure-note"><ShieldCheck/>情報は暗号化通信で安全に送信されます</p></section></section></main>}
