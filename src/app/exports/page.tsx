import {CalendarRange,Download,FileSpreadsheet} from "lucide-react";
import {AppHeader} from "@/components/app-header";
import {requireUser} from "@/server/authz";
const cards=[{kind:"participants",title:"参加者CSV",description:"連絡先、申込イベント、チケット、入金・参加状態"},{kind:"payments",title:"入金CSV",description:"入金状態、振込名義、入金日（売上金額はスーパー管理者のみ）"},{kind:"checkins",title:"受付CSV",description:"受付日時、参加者、チケット、受付方法、担当者"},{kind:"staff",title:"スタッフ実績CSV",description:"確保・申込・入金・受付人数と残枚数"}];
export default async function ExportsPage({searchParams}:{searchParams:Promise<{year?:string}>}){const user=await requireUser(["SUPER_ADMIN","EVENT_MANAGER","RECEPTION_STAFF"]);const q=await searchParams;const year=/^20\d{2}$/.test(q.year??"")?Number(q.year):new Date().getFullYear();const visible=user.role==="RECEPTION_STAFF"?cards.filter(c=>c.kind==="checkins"):cards;return <><AppHeader active="exports"/><main className="content-shell"><div className="page-heading"><div><p className="eyebrow">DATA EXPORT</p><h1>CSV出力</h1><p>業務データをExcel対応のUTF-8 CSVでダウンロードします。</p></div><form className="year-picker"><CalendarRange/><input name="year" type="number" min="2020" max="2100" defaultValue={year}/><button>変更</button></form></div><section className="export-grid">{visible.map(card=><article key={card.kind}><FileSpreadsheet/><div><h2>{card.title}</h2><p>{card.description}</p></div><a href={`/api/exports/${card.kind}?year=${year}`}><Download/>{year}年をダウンロード</a></article>)}</section><p className="export-note">CSVには個人情報が含まれます。必要な端末だけに保存し、利用後は安全に削除してください。</p></main></>}
