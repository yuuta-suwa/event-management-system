import {createHmac} from "node:crypto";
import {describe,expect,it} from "vitest";
import {notificationMessage,verifyLineSignature} from "@/features/notifications/domain";
describe("notification domain",()=>{it("LINE署名を検証する",()=>{const body='{"events":[]}';const secret="secret";const signature=createHmac("sha256",secret).update(body).digest("base64");expect(verifyLineSignature(body,signature,secret)).toBe(true);expect(verifyLineSignature(body,"invalid",secret)).toBe(false)});it("未入金通知に振込先を含める",()=>expect(notificationMessage("PAYMENT_REMINDER",{name:"山田",eventName:"交流会",startTime:new Date(),venueName:"会場",bankInformation:"銀行口座"})).toContain("銀行口座"));it("開催前通知に受付時刻とQRを含める",()=>{const message=notificationMessage("EVENT_THREE_HOURS",{name:"山田",eventName:"交流会",startTime:new Date("2026-09-10T10:00:00Z"),receptionStartTime:new Date("2026-09-10T09:30:00Z"),venueName:"会場",bankInformation:"口座",ticketUrl:"https://example.com/ticket"});expect(message).toContain("受付開始");expect(message).toContain("https://example.com/ticket")})});
