import {describe,expect,it} from "vitest";
import {hashTicketToken,issueTicketToken,verifyTicketToken} from "@/features/tickets/token";
const secret="test-ticket-secret-is-longer-than-32-characters";
describe("ticket QR token",()=>{it("個人情報を含まず発行・検証できる",()=>{const token=issueTicketToken("ticket-123",1,secret);expect(token).not.toContain("山田");expect(verifyTicketToken(token,secret)).toEqual({ticketId:"ticket-123",version:1})});it("改ざんを拒否する",()=>{const token=issueTicketToken("ticket-123",1,secret);expect(verifyTicketToken(`${token}x`,secret)).toBeNull()});it("異なる秘密鍵を拒否する",()=>{const token=issueTicketToken("ticket-123",1,secret);expect(verifyTicketToken(token,"another-secret-that-is-longer-than-32-characters")).toBeNull()});it("バージョン更新で別トークンになる",()=>expect(issueTicketToken("ticket-123",1,secret)).not.toBe(issueTicketToken("ticket-123",2,secret)));it("DB保存用ハッシュが再現可能",()=>{const token=issueTicketToken("ticket-123",1,secret);expect(hashTicketToken(token)).toHaveLength(64)})});
